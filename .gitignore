node_modules/
pnpm-lock.yaml
.DS_Store

# 插件操作日志：默认就落在启动 dsh 时的工作区目录（process.cwd()），
# 也就是这份插件源码所在的目录 —— 只在本机看，不入库。
/git-panel.log
/git-panel.log.1
*.log

# 版面自查（不进仓库）：用假 React 驱动真实 client bundle、headless Chrome 截图。
# 目录里是 .ui-preview/preview.cjs 与几张 light/dark/tall 截图，随时可删。
/.ui-preview/
